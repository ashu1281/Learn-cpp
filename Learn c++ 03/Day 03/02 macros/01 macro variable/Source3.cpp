#include "macros.h"
int main() {
	int radius = 10;
	double area = PI * radius * radius;
	double volume = 4.0 / 3.0 * PI * radius * radius * radius;
}
/*
-macro definitions are placed in header file.
-here we include the macros.h file which are present in same directory.
*/
