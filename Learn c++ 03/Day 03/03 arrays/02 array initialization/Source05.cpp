#define N 3
#define R 2
#define C 3

void main() {
	int a[N] = { 1 };
	int b[R][C] = { 1 };
	int c[R][C] = { 1 };
}

/*
* This syntax doesn't set all elements to one.
* Only first element is set to one, rest all elements are set to zero.
*/
