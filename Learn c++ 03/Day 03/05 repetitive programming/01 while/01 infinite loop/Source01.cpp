#include <iostream>
using namespace std;

void main() {
	while (true)
		cout << "Hello, World" << endl;
}

/*
* while is a pre test loop. 
* Pre test loop is a loop in which test expression is evaluated first and then the loop body is executed.
* If test expression evaluates to true, the loop body is repeated else it is terminated.
* while(true) results into an infinite loop.
*/
