int main() {
	int a = 1;
	int* pa = nullptr;
	pa = &a; // pointer assignment
}

/*
- When we do not have any particular address to store
  in pointer variable then we prefer to store nullptr(0)
  in pointer variable.
////- Such pointer is called as 'null pointer'.
*/
