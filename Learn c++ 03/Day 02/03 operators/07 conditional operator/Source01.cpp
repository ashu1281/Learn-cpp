void main() {
	int a = 10, b = -32, c = 0;
	c = a > b ? a : b;
}

/*
What is general syntax of "?:"?
* variable = condtion ? true-expression : false-expression;
* Condition can be simple or complex.
* If condition evaluates to true, true-expression is evaluated and result is assigned to variable.
* Else false-expression is evaluated and result is assigned to variable.
* Mentioning of variable is optional.
*/
