class A {};

class A {};

int main() {

}

/*
- If type (class/struct/union/enum) is repeated and an attempt is made to
  build an executable, then it fails.
- Thus, it implies compiler doesn't allow repetition of type.
*/
